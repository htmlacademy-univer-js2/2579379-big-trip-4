const HOURS_PER_DAY = 24;
const MINUTES_PER_HOUR = 60;

const Formats = {
  DURATION: 'HH:mm',
  VALUE: 'YYYY-MM-DD',
  DAY: 'MMM D',
  FULL_DATE: 'D/MM/YY HH:mm',
};

export { HOURS_PER_DAY, MINUTES_PER_HOUR, Formats };
